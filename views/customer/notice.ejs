<!DOCTYPE html>
<html lang = "en">
<head>
	<title><%= storeInfo.shopName %> 공지사항</title>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no" />
	<script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
</head> // API 확인하기
<style>
    #regDiv {
  margin-left: auto;
  margin-right: auto;
  width: 1000px;
  height: 700px;
  text-align: center;
  border-radius: 6px;
}
input:-webkit-autofill {
  -webkit-box-shadow: 0 0 0 1000px white inset;
}

#regDiv input {
  width: 300px;
  height: 50px;
  background-color: ghostwhite;
  border: solid 1px lightgray;
}

#regDiv input[type="radio"] {
  width: auto;
  height: auto;
  text-align: left;
}

#regDiv input[type="submit"] {
  width: 100%;
  background-color: #ff9763;
  color: white;
}

#regDiv table {
  margin-left: auto;
  margin-right: auto;
}
#regDiv td:nth-child(1) {
  text-align: right;
}
#regDiv td:nth-child(3) {
  text-align: left;
}
#regDiv td:nth-child(2n + 1) {
  width: 200px;
}
#regDiv button {
  background-color: mediumslateblue;
  color: white;
  width: 150px;
  height: 45px;
  border: 0px;
}

#loginDiv img {
  margin-top: 100px;
  margin-bottom: 100px;
  margin-left: auto;
  margin-right: auto;
}

</style>
<body>
	<header>
		<%- include('navbar.ejs')%>
	</header>
	<section>
        <div style="margin-left:auto; margin-right:auto; width:800px">
        <% for(let i=0;i<noticeList.length;i++){%>
            <div style="border:1px solid black">
                <% if(noticeList[i].isEvent==0) { %>
                    <h3>[공지사항]&nbsp;<%= noticeList[i].title %></h3>
                    <div style="text-align: right;"><%= String(noticeList[i].registrationDate).substr(0,10) %></div>
                <% } else{%>
                    <h3>[이벤트]&nbsp;<%= noticeList[i].title %></h3><%= String(noticeList[i].registrationDate).substr(0,10) %>
                <%} %>
            <img src="<%=noticeList[i].image%>" style="width:800px;height:800px"></td>
            </div>
        <% } %>
        </div>
	</section>
	<footer>
		<%- include ('footer') %>
	</footer>
</body>
</html>